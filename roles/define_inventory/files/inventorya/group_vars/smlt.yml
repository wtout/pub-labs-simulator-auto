---
# Variables for standalone VMs
credentials:
  username: "{{ NONEM7_USER|default('') }}"
  password: "{{ NONEM7_PASS|default('') }}"

user:
  name: "{{ vm_vars.smlt.user.name if vm_vars.smlt.user.name is defined else '' }}"
  password: "{{ vm_vars.smlt.user.password if vm_vars.smlt.user.password is defined else '' }}"

vm:
  cpu: "{{ vm_vars.smlt.cpu }}"
  memory: "{{ vm_vars.smlt.memory }}"
  disk0: "{{ vm_vars.smlt.disk0 }}"
  disk1: "{{ vm_vars.smlt.disk1 | default(0) }}"
  disk2: "{{ vm_vars.smlt.disk2 | default(0) }}"
  disk3: "{{ vm_vars.smlt.disk3 | default(0) }}"
  disk_provisioning: "{{ vm_vars.smlt.disk_provisioning if vm_vars.smlt.disk_provisioning != '' else '' }}"
  hostmap: "{{ vm_vars.smlt.hostmap | default([]) }}"
  name: "{{ vm_vars.smlt.vmname if vm_vars.smlt.vmname != '' else (build.primary.name_prefix + inventory_hostname[-2:]) }}"

netconfig:
  nic1:
    intname: "{{ vm_vars.smlt.nic1.intname }}"
    ippool: "{{ vm_vars.smlt.nic1.ip_pool | default([]) }}"
    octetpool: "{{ vm_vars.smlt.nic1.ip_octetpool | default([]) }}"
    gateway: "{{ vm_vars.smlt.nic1.gtw if vm_vars.smlt.nic1.gtw != '' else (build.primary.octets + '.' + vm_vars.smlt.nic1.gtw_octet) if vm_vars.smlt.nic1.gtw_octet != '' else '' }}"
    netmask: "{{ vm_vars.smlt.nic1.netmask }}"
    network: "{{ vm_vars.smlt.nic1.netname if vm_vars.smlt.nic1.netname != '' else (build.primary.name_prefix + '-' + vm_vars.smlt.nic1.netbase) if vm_vars.smlt.nic1.netbase != '' else '' }}"
    ipv6_pool: "{{ vm_vars.smlt.nic1.ipv6_pool | default([]) }}"
    ipv6_gtw: "{{ vm_vars.smlt.nic1.ipv6_gtw | default('') }}"

  nic2:
    intname: "{{ vm_vars.smlt.nic2.intname }}"
    ippool: "{{ vm_vars.smlt.nic2.ip_pool | default([]) }}"
    octetpool: "{{ vm_vars.smlt.nic2.ip_octetpool | default([]) }}"
    gateway: "{{ vm_vars.smlt.nic2.gtw if vm_vars.smlt.nic2.gtw != '' else (build.primary.octets + '.' + vm_vars.smlt.nic2.gtw_octet) if vm_vars.smlt.nic2.gtw_octet != '' else '' }}"
    netmask: "{{ vm_vars.smlt.nic2.netmask }}"
    network: "{{ vm_vars.smlt.nic2.netname if vm_vars.smlt.nic2.netname != '' else (build.primary.name_prefix + '-' + vm_vars.smlt.nic2.netbase) if vm_vars.smlt.nic2.netbase != '' else '' }}"
    ipv6_pool: "{{ vm_vars.smlt.nic2.ipv6_pool | default([]) }}"
    ipv6_gtw: "{{ vm_vars.smlt.nic2.ipv6_gtw | default('') }}"
