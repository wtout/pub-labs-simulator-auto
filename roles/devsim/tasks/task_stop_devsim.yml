---
# tasks to stop device simulation
- block:
  - include_tasks:
      file: task_manage_avm.yml
      apply:
        vars:
          avm_id: "{{ id_item }}"
          avm_action: 'stop'
    loop: "{{ good_avmid_list }}"
    loop_control:
      loop_var: id_item
  tags: [ 'never', 'devsim' ]
